import { Task } from '../types';

/**
 * Danh sách công việc hệ thống (Sạch, không có dữ liệu demo)
 */
export const INITIAL_58_TASKS: Task[] = [];

export const INITIAL_TASKS: Task[] = INITIAL_58_TASKS;
